export type ChatMessage = {
    id: string;
    text: string;
    username: string;
    createdAt: string;
};

export type ChatRoom = {
    id: string;
    roomname: string;
    createdAt: string;
};
